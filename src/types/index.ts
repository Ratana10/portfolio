export interface IconType {
  name: string;
  src: string;
}

export interface Experience{
  company: string;
  role: string;
  duration: string;
  description: string;
  skills: string[];
  logo : string;
}